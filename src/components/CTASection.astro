---
interface Props {
  title: string;
  description: string;
  primaryButtonText: string;
  secondaryButtonText: string;
}

const { title, description, primaryButtonText, secondaryButtonText } = Astro.props;
---

<section class="bg-gradient-to-r from-indigo-600 to-blue-700 rounded-2xl overflow-hidden">
  <div class="px-8 py-16 md:px-16 text-center">
    <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">
      {title}
    </h2>
    <p class="text-xl text-indigo-100 mb-8 max-w-2xl mx-auto">
      {description}
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <button class="px-8 py-3 bg-white text-indigo-600 rounded-lg font-semibold hover:bg-indigo-50 transition-colors">
        {primaryButtonText}
      </button>
      <button class="px-8 py-3 bg-transparent border-2 border-white text-white rounded-lg font-semibold hover:bg-white/10 transition-colors">
        {secondaryButtonText}
      </button>
    </div>
  </div>
</section>
